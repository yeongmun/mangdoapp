node_modules/
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# ENV - API 키는 절대 커밋하지 않기
.env
.env.*
!.env.example
server/data/

# macOS
.DS_Store

# 사내 도면 원본은 커밋하지 않는다 (치수는 스펙에 적어 둔다)
*.dwg
*.dxf
# 테스트 픽스처 DXF는 손으로 쓴 작은 파일이라 커밋한다 (사내 도면이 아니다)
!server/test/fixtures/*.dxf
*.bak
*.dwl
*.dwl2
